 float altura, peso, resultado, resultado2 ;
  
  printf("Digite sua altura. \n");
  scanf("%f.", &altura);
  printf("Digite sua peso. \n");
  scanf("%f.", &peso);
  
  resultado = (altura * altura);
  
  resultado2 = peso / resultado;
  printf("Seu imc é de %f \n", resultado2);
    if (resultado)
    
